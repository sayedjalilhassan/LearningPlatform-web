@model IEnumerable<LearningPlatform.Models.Course>
@{
    ViewBag.Title = "Browse";
}
<div class="well container">
    <h3>
        Browse Courses</h3>
    <p>
        Select from @Model.Count() courses:</p>
    <ul class="thumbnails">
        <!-- List start -->
        @foreach (var course in Model)
        {
@* <li class="span3">
                <div class="thumbnail">
                    <a href="@Url.Action("Browse", new { course = course.CourseName })">
                        <div class="caption">
                            <h5>@course.CourseName</h5>
                        </div>
                        <div class="thumbnail">
                            <img alt="@course.CourseName" src="/Content/Images/placeholder.gif" />
                        </div>
                    </a>
                </div>
            </li>*@
            
            <li class="span4 well">
                <fieldset>
                    <h4>
                        Course: @course.CourseName</h4>
                    <div class="row">
                        <div class="span1">
                            <img src="/content/Images/placeholder.gif" alt="" /></div>
                        <div class="span3">
                            <p>
                                Instructor:
                            </p>
                            <p>
                                <strong>Pervaiz HoodBhoy</strong></p>
                            <p>
                                Department:
                            </p>
                            <p>
                                <strong>@course.Department.DepartmentName</strong></p>

                            <a href="@Url.Action("Browse", new { course = course.CourseName })"><span class=" badge badge-warning">
                                Details</span> </a>
                        </div>
                    </div>
                </fieldset>
            </li>
        }
    </ul>
</div>
